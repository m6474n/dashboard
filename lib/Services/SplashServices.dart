import 'dart:async';

import 'package:dashboard/Screens/AuthenticationPage.dart';
import 'package:get/get.dart';

class SplashService {
  void isLogin() {
    Timer(Duration(seconds: 3), () {
      // Get.to(AuthenticationScren());
    });
  }
}
